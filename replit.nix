{ pkgs }: {
	deps = [
        pkgs.unzip
        pkgs.vim
        pkgs.sudo
        pkgs.wget
        pkgs.nano
        pkgs.ruby_3_0
        pkgs.rubyPackages_3_0.solargraph
        pkgs.rufo
        pkgs.sqlite
	pkgs.chromium
	];
}
