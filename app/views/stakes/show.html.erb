<h1>Employee View</h1>

<div id="stakes">
  <% if !@stakes.nil? %>
    <% @stakes.each do |stake| %>
      <tr>
        <p><%= stake.user_name %> <%=Company.get_company_name_by_id(stake.c_id) %> <%=stake.value %></p>
      </tr>
      <% end %>
  <% end %>
</div>

